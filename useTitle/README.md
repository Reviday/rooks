# Usage

```js
import useTitle from "@rooks/use-title";

const App = () => {
  const titleUpdater = useTitle("Loading...");
  return (
    <div className="App">
      <div>Hi</div>
    </div>
  );
};
```
